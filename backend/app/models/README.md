# app/models

Pydantic schemas (request/response bodies, data shapes) go here — e.g. the
clothing item tag schema (category, color, pattern, season, occasion) once
the `/label` endpoint is implemented.

Not to be confused with the top-level [`/models`](../../models) directory,
which holds the future custom vision model itself.
